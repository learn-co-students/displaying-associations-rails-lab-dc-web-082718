<!-- <h1>Artists</h1>

<h2>
<ul>


  <%@artists.each do |artist|%>
  <li><%=artist.name%></li>
  <li><%=artist.song_count%>songs</li>
  <%end%> -->








</ul>
</h2>

<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <li>
      <%= link_to artist.name, artist_path(artist) %>
      (<%= artist.song_count %> <%= "song".pluralize(artist.song_count) %>)
    </li>
  <% end %>
</ul>
